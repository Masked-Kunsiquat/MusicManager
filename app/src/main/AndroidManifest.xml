<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MusicManager">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Fires when DownloadManager completes any download; GemmaLiteRtProvider.initialize()
             checks if the model file exists before doing any real work. -->
        <receiver
            android:name=".ai.DownloadCompleteReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.DOWNLOAD_COMPLETE" />
            </intent-filter>
        </receiver>

        <!-- Debug only: adb shell am broadcast -a com.github.maskedkunisquat.DEBUG_TICK -p <pkg> -->
        <receiver
            android:name=".DebugTickReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.github.maskedkunisquat.DEBUG_TICK" />
            </intent-filter>
        </receiver>

        <!-- Debug only: clears DB + prefs WITHOUT touching the model file -->
        <receiver
            android:name=".DebugResetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.github.maskedkunisquat.DEBUG_RESET" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
